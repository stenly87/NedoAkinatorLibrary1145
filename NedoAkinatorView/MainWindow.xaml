<Window x:Class="NedoAkinatorView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NedoAkinatorView"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="Список персонажей" Click="OpenCharacters" />
            <MenuItem Header="Список вопросов" Click="OpenQuestions" />
            <Button Content="Старт" Click="Start"/>
        </Menu>
        <Grid x:Name="gridHost" Grid.Row="1"/>
    </Grid>
</Window>
